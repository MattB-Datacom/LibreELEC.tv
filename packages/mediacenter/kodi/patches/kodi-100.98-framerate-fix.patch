--- a/xbmc/cores/VideoPlayer/VideoPlayer.cpp	2017-05-28 04:45:31.000000000 +1200
+++ b/xbmc/cores/VideoPlayer/VideoPlayer.cpp	2017-06-05 23:44:15.000000000 +1200
@@ -3826,7 +3826,19 @@
   {
     if (CServiceBroker::GetSettings().GetInt(CSettings::SETTING_VIDEOPLAYER_ADJUSTREFRESHRATE) != ADJUST_REFRESHRATE_OFF)
     {
-      double framerate = DVD_TIME_BASE / CDVDCodecUtils::NormalizeFrameduration((double)DVD_TIME_BASE * hint.fpsscale / hint.fpsrate);
+      double framerate;
+
+      if(hint.fpsscale == 21 && hint.fpsrate == 500)
+      {
+          framerate = 23.976024;
+      }
+      else
+      {
+          framerate = DVD_TIME_BASE / CDVDCodecUtils::NormalizeFrameduration((double)DVD_TIME_BASE * hint.fpsscale / hint.fpsrate);
+      }
+        
+      CLog::Log(LOGNOTICE, "VideoPlayer: About to TriggerUpdateResoltion. framerate = %f.", framerate);
+
       m_renderManager.TriggerUpdateResolution(static_cast<float>(framerate), hint.width, RenderManager::GetStereoModeFlags(hint.stereo_mode));
     }
   }
